from setuptools import setup, find_packages

setup(
    name='opented',
    version='2.0',
    description="Tenders electronic daily for the EU, data dump web site.",
    long_description='',
    classifiers=[
        ],
    keywords='',
    author='',
    author_email='friedrich@pudo.org',
    url='http://opented.org',
    license='GPL3',
    packages=find_packages(exclude=['ez_setup', 'examples', 'tests']),
    namespace_packages=[],
    include_package_data=False,
    zip_safe=False,
    install_requires=[
    ],
    tests_require=[],
    entry_points=\
    """ """,
)
